---

- name: Uninstall system micromamba
  hosts: workstations
  remote_user: "{{ admin_user }}"
  become: true
  become_user: root
  become_method: sudo
  tasks:
    - name: Uninstall system micromamba
      include_role:
        name: maintenance
        tasks_from: uninstall_system_micromamba



