---

- name: Deploy itk-snap
  hosts: workstations
  remote_user: "{{ admin_user }}"
  become: true
  become_user: "{{ software_user }}"
  become_method: sudo
  tasks:
    - name: Install itk-snap
      include_role:
        name: local
        tasks_from: itksnap
